{
  "buildCommand": "bun install",
  "installCommand": "curl -fsSL https://bun.sh/install | bash",
  "functions": {
    "src/index.ts": {
      "maxDuration": 30,
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/src"
    }
  ]
}